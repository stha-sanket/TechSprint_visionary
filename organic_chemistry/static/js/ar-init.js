AFRAME.registerComponent("ar-init", {
  init() {
    console.log("AR scene initialized");

    // This is intentionally empty for now.
    // WebXR / hit-test will be added ONLY after desktop scene works.
  }
});
